#!/bin/sh

# Lets just asuume we always run this from the project directory.
# otherwise the path complexity is a big headache for little benefit.
# jgreve, 2018-05-10
# ugly: # Absolute path to this script, e.g. /home/user/bin/foo.sh
# ugly: SCRIPT=$(readlink -f "$0")
# ugly: # Absolute path this script is in, thus /home/user/bin
# ugly: SCRIPTPATH=$(dirname "$SCRIPT")

# to do: hook this info from a config file, maybe ask python
# to generate a *.env based on a config file (e.g. *.yml to shell env-var).
# check our output directory

set -u
# blow up on undefined env-vars

source bin/summ.env
source $BIN_DIR/clean_run

$SRC_DIR/summarizer.py $@
